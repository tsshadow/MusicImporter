{
  "name": "frontend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "VITE_API_BASE=http://localhost:8000/api vite dev",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^3.0.0",
    "@sveltejs/kit": "^2.5.0",
    "svelte": "^4.2.0",
    "vite": "^5.0.0"
  }
}
